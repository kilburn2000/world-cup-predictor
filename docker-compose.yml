# Run the whole app locally: the Fastify+React container plus a Postgres.
#
#   docker compose up --build            # start db + app
#   docker compose exec app npm run db:push   # create the schema (first run)
#   docker compose exec app npm run seed      # seed reference data (first run)
#
# Then open http://localhost:8790. Put secrets (ANTHROPIC_API_KEY, ADMIN_*, etc.)
# in a local .env file - it is git/dockerignored and read via env_file below.

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: worldcup
      POSTGRES_USER: worldcup
      POSTGRES_PASSWORD: worldcup
    ports:
      - "5432:5432"
    volumes:
      - wc-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U worldcup -d worldcup"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    ports:
      - "8790:8790"
    # These are set here (not via .env) on purpose: the compose app always talks
    # to the bundled `db` service. `environment:` wins over `env_file:`, so a
    # DATABASE_URL in .env is intentionally ignored - to point at an external
    # Postgres, edit the line below or run the image without compose (see
    # DOCKER.md). Kept literal (not ${DATABASE_URL:-...}) so a stray DATABASE_URL
    # in your shell can't redirect the container off the bundled db.
    environment:
      NODE_ENV: production
      PORT: 8790
      DATABASE_URL: postgres://worldcup:worldcup@db:5432/worldcup
      VISION_MODEL: claude-sonnet-4-6
    # Optional secrets (ANTHROPIC_API_KEY, ADMIN_TOKEN, ADMIN_EMAIL,
    # ADMIN_PASSWORD_HASH, FOOTBALL_DATA_TOKEN). Absent .env is fine.
    # NOTE: the long-form env_file (path/required) needs Docker Compose >= 2.24.
    env_file:
      - path: .env
        required: false
    depends_on:
      db:
        condition: service_healthy

volumes:
  wc-pgdata:
